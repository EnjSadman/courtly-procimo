services:
  db:
    image: postgres:16
    container_name: courtly-postgres
    environment:
      POSTGRES_USER: courtly
      POSTGRES_PASSWORD: courtly
      POSTGRES_DB: courtly
    ports:
      - "5432:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U courtly -d courtly"]
      interval: 5s
      timeout: 5s
      retries: 20
    volumes:
      - courtly-postgres-data:/var/lib/postgresql/data

volumes:
  courtly-postgres-data:
